{
  "name": "bus-booking-platform",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @repo/events build && pnpm --filter @repo/observability build && turbo run build",
    "dev": "pnpm --filter @repo/events build && pnpm --filter @repo/observability build && turbo run dev --filter=web --filter=api-gateway --filter=trip-service --filter=booking-service --filter=payment-service",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "db:setup": "pnpm --filter trip-service prisma:generate && pnpm --filter booking-service prisma:generate && pnpm --filter payment-service prisma:generate && pnpm --filter trip-service exec prisma migrate deploy && pnpm --filter booking-service exec prisma migrate deploy && pnpm --filter payment-service exec prisma migrate deploy && pnpm --filter trip-service prisma:seed"
  },
  "devDependencies": {
    "prettier": "3.9.6",
    "turbo": "^2.10.12",
    "typescript": "7.0.2"
  },
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=24"
  }
}
